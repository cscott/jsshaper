// Adjust paths for node; define Iterator and StopIteration globally.
require && require.paths && typeof __dirname !== "undefined" && require.paths.unshift(__dirname+"/../..");

// put Iterator in global scope
require('iterator.js');
// put StopIteration in global scope
require('stopiteration.js');
